<div class="modal-header">
    <h3>Añadir obra a lista</h3>
</div>

<form name="form.bookForm" ng-submit="submitForm()" novalidate>
    <div class="modal-body">
        <!-- TITLE --> 
        <div class="form-group">
            <label for="title">Título</label>
            <input type="text" ng-model="title" placeholder="Obra..." typeahead-on-select="setWorkValues($item)" typeahead="work.title for work in getWork($viewValue)" typeahead-loading="loadingWorks" typeahead-no-results="noResults" class="form-control" required>
            <i ng-show="loadingWorks" class="glyphicon glyphicon-refresh"></i>
            <div ng-show="noResults">
                <i class="glyphicon glyphicon-remove"></i> No se han encontrado resultados
            </div>
            <p ng-show="form.bookForm.title.$invalid && !form.bookForm.title.$pristine" class="help-block">Introduzca una obra.</p>
        </div>

        <div class="media" ng-show="cover">
            <div class="media-left media-middle">
                <a ng-href="{{url}}" target="_blank">
                  <img tooltip-placement="top" tooltip="Ver obra en catálogo" width="64px" class="media-object" ng-src="http://media.cervantesvirtual.com/s3/BVMC_OBRAS/{{cover}}/portada/Cover.jpg" ng-if="cover" alt="...">
                </a>
            </div>
            <div class="media-body">
                <label>Descripción</label>
                <p>{{title}}</p>
                <p>{{reproduction}}</p>
                <p>{{language}}</p>
                <p>{{mediatype}}</p>
                <p></p>
                </div>
            </div>

            <!-- Comments -->
            <div class="form-group">
                <label for="comments">Comentarios</label>
                <textarea name="comments" ng-model="comments" id="comments" class="form-control" cols="20" rows="10" placeholder="Comentarios"></textarea>
            </div>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary" ng-disabled="form.bookForm.$invalid">Guardar</button>
        <button class="btn btn-warning" ng-click="cancel()">Cancelar</button>
    </div>
</form>
