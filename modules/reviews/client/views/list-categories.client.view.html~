<section ng-controller="CategoriesController" ng-init="find()">
  <div class="page-header">
    <h1>Categories</h1>
  </div>
  <div class="list-group">
    <a ng-repeat="category in categories" ui-sref="categories.view({categoryId: category._id})" class="list-group-item">
      <!--<small class="list-group-item-text">
        Posted on
        <span ng-bind="category.created | date:'mediumDate'"></span>
        by
        <span ng-bind="category.user.displayName"></span>
      </small>-->
      <h4 class="list-group-item-heading" ng-bind="category.title"></h4>
      <p class="list-group-item-text" ng-bind="category.content"></p>
    </a>
  </div>
  <div class="alert alert-warning text-center" ng-if="categories.$resolved && !categories.length">
    No categories yet, why don't you <a ui-sref="categories.create">create one</a>?
  </div>
</section>
