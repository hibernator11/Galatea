<section ng-controller="BooksController" ng-init="findOne()">
  <div class="page-header">
    <h1 ng-bind="book.name"></h1>
  </div>
  <div class="pull-right" ng-show="authentication.user._id == book.user._id">
    <a class="btn btn-primary" ui-sref="books.edit({bookId: book._id})">
      <i class="glyphicon glyphicon-edit"></i>
    </a>
    <a class="btn btn-primary" ng-click="remove();">
      <i class="glyphicon glyphicon-trash"></i>
    </a>
  </div>
  <!--<small>
    <em class="text-muted">
      Posted on
      <span ng-bind="category.created | date:'mediumDate'"></span>
      by
      <span ng-bind="category.user.displayName"></span>
    </em>
  </small>-->
  <p class="lead" ng-bind="book.description"></p>
</section>
