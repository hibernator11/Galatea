<div class="modal-header">
    <h3 class="modal-title pull-left">Añadir obra a lista</h3>
    <button ng-click="cancel()" type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
</div>

<form name="form.bookForm" ng-submit="submitForm()" novalidate>
    <div class="modal-body">
        <!-- TITLE --> 
        <div class="form-group">
            <label for="title">Busca una obra por su título</label>
            <input type="text" ng-model="title" placeholder="Busca un título en el catálogo" typeahead-on-select="setWorkValues($item)" typeahead="work.title for work in getWork($viewValue)" typeahead-loading="loadingWorks" typeahead-no-results="noResults" class="form-control" required>
            <i ng-show="loadingWorks" class="glyphicon glyphicon-refresh"></i>
            <div class="alert alert-warning" style="margin-top:5px;" ng-show="noResults">
                <strong>Aviso:</strong> No se han encontrado resultados. Introduce un título de nuestro catálogo por favor.
            </div>
            <p ng-show="form.bookForm.title.$invalid && !form.bookForm.title.$pristine" class="help-block">Introduzca una obra.</p>
            <span id="helpBlock" class="help-block"><span class="glyphicon glyphicon-info-sign"></span> Comienza a escribir un título del catálogo de nuesta biblioteca y selecciona una opción.</span>
        </div>

        <div class="well well-sm" ng-show="cover">
          <div class="row">
            <div class="col-xs-3 col-md-3 text-center">
               <img ng-if="cover" ng-src="http://media.cervantesvirtual.com/s3/BVMC_OBRAS/{{cover}}/portada/Cover.jpg" alt="Obra seleccionada" class="img-rounded img-responsive" />
            </div>
            <div class="col-xs-9 col-md-9 section-box">
              <h4><a ng-href="{{url}}" target="_blank">Ver obra en catálogo <span class="glyphicon glyphicon-new-window"></span></a></h4>
              <p>{{ title | limitTo: 200 }}{{title.length > 200 ? '...' : ''}}</p>
              <p class="text-muted" ng-show="reproduction" ng-bind-html="reproduction | htmlLimit"></p>
              <div class="row rating-desc">
                <div class="col-md-12">
                  <span class="label label-info" ng-show="language">{{language}}</span>
                  <span class="label label-info" ng-show="mediatype">{{mediaType}}</span>
                </div>
              </div>
            </div>
          </div>
        </div>

            <!-- Comments -->
            <div class="form-group">
                <label for="comments">Comentarios de la obra</label>
                <textarea name="comments" ng-model="comments" id="comments" class="form-control" cols="20" rows="10" placeholder="Comentarios"></textarea>
            </div>
    </div>
    <div class="modal-footer">
        <button type="submit" class="btn btn-primary" ng-disabled="form.bookForm.$invalid">Guardar</button>
        <button class="btn btn-warning" ng-click="cancel()">Cancelar</button>
    </div>
</form>
